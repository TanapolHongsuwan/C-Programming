#include <stdio.h>

// xをyで割った商と余りを，
// それぞれquotientとremainderが指す変数に格納する関数
void divide(int x, int y, int *quotient, int *remainder) {


}

int main(void) {
    int x, y, q, r;

    printf("xを入力: "); scanf("%d", &x);
    printf("yを入力: "); scanf("%d", &y);

    divide(x, y,                      );

    printf("商: %d, 余り: %d\n", q, r);

    return 0;
}



